# Keep VSIX lean: ship only the bundled runtime in dist/ plus metadata files.
# Required files: package.json, README.md, LICENSE.txt, CHANGELOG.md, dist/extension.js, dist/extension.meta.json, beady-visual.png

# Workspace + tooling scaffolding
.vscode/**
.vscode-test/**
.git/**
.beads/**
.claude/**
.github/**
tmp/**

# Source and build outputs not needed at runtime
src/**
out/**
packages/**
web/**
tui/**
docs/**
test/**
scripts/**

# Dependencies bundled into dist/
node_modules/**

# Dev artifacts and maps
**/*.ts
**/*.map
.eslintrc.json
.eslintignore
tsconfig.json
tsconfig.*.json
package-lock.json

# Test/dev config files
.c8rc.json
.mocharc.js
.heartbeat.pid
.gitignore

# Dev documentation (keep README.md only)
AGENTS.md
CONTRIBUTING.md
INTERFACE_DESIGN.md
QUICKSTART.md
TESTING.md
notes.md

# Only include required nls file
# package.nls.json is needed for localization

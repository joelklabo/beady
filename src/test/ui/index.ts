/**
 * UI Test Runner Configuration
 * 
 * Currently, UI tests are run using the `extest` CLI tool via `npm run test:ui`.
 * This file is reserved for future programmatic test runner setup or shared utilities.
 */

export {};
